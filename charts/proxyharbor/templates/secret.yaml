{{- if not .Values.auth.existingSecret }}
{{- fail "auth.existingSecret is required" }}
{{- end }}
{{- if and (eq .Values.storage "mysql") (not .Values.mysql.existingSecret) }}
{{- fail "mysql.existingSecret is required when storage=mysql" }}
{{- end }}
